package com.minecraftclone.ui;

import javax.swing.*;
import java.awt.*;

public class ChatFrame extends JFrame {

    public ChatFrame() {
        super("Chat");
        setSize(400, 300);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);

        // Finestra vuota
        // Potresti aggiungere componenti per la chat in futuro
    }
}
